# =====================================================
# Family Budget Manager - 환경변수 템플릿
# =====================================================
# 이 파일을 복사하여 .env.local로 이름을 변경하고
# 실제 Firebase 프로젝트 값으로 교체하세요.
#
# 설정 방법:
# 1. Firebase Console (https://console.firebase.google.com) 접속
# 2. 프로젝트 설정 > 일반 > 내 앱 > Firebase SDK snippet
# 3. 구성(Config) 값을 아래에 입력
# =====================================================

# -----------------------------------------------------
# Firebase 설정 (필수)
# -----------------------------------------------------
# Firebase Console > 프로젝트 설정 > 일반 > 웹 앱에서 확인
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# -----------------------------------------------------
# Firebase Analytics (선택)
# -----------------------------------------------------
# Google Analytics 사용 시 설정
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# -----------------------------------------------------
# Firebase Emulator 설정 (개발 환경 전용)
# -----------------------------------------------------
# 로컬 개발 시 Firebase Emulator 사용 여부
# true로 설정하면 로컬 에뮬레이터에 연결됩니다.
NEXT_PUBLIC_USE_FIREBASE_EMULATOR=false

# Emulator 호스트/포트 (기본값 사용 시 설정 불필요)
# NEXT_PUBLIC_FIREBASE_AUTH_EMULATOR_HOST=localhost
# NEXT_PUBLIC_FIREBASE_AUTH_EMULATOR_PORT=9099
# NEXT_PUBLIC_FIREBASE_FIRESTORE_EMULATOR_HOST=localhost
# NEXT_PUBLIC_FIREBASE_FIRESTORE_EMULATOR_PORT=8080
# NEXT_PUBLIC_FIREBASE_STORAGE_EMULATOR_HOST=localhost
# NEXT_PUBLIC_FIREBASE_STORAGE_EMULATOR_PORT=9199

# -----------------------------------------------------
# 앱 설정 (선택)
# -----------------------------------------------------
# 앱 이름 (기본값: Family Budget Manager)
# NEXT_PUBLIC_APP_NAME=Family Budget Manager

# -----------------------------------------------------
# 주의사항
# -----------------------------------------------------
# - .env.local 파일은 절대 Git에 커밋하지 마세요!
# - 프로덕션 배포 시 Vercel/Netlify 등의 환경변수 설정 사용
# - NEXT_PUBLIC_ 접두사가 있어야 클라이언트에서 접근 가능
